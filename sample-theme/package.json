{
  "name": "sample-theme",
  "version": "1.0.0",
  "description": "Glassmorphism dark LP with Tailwind and Three.js starfield",
  "scripts": {
    "dev": "tailwindcss -i ./src/styles.css -o ./style.css --watch",
    "build": "tailwindcss -i ./src/styles.css -o ./style.css --minify"
  },
  "keywords": ["wordpress", "theme", "tailwind", "threejs"],
  "author": "Bolt.new",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0"
  }
}
